
Supported Hubs
=========================

``iot-automator`` does not handle any native connections to IoT devices (sensors, smart plugs, bulbs, etc.). Instead, it relies on a set of "hubs" to do the actual work. Currently, the following hubs are supported:


Click on any of the hubs in the drop down menu to explore how that hub's connection and API calls are abstracted into a simple, easy-to-use interface in the ``iot-automator`` library.


.. toctree::

   webthings
   home_assistant
   openhab
   alexa
   google_home
   smartthings


